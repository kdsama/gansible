all:  # This is the top-level group, which contains all hosts
  hosts:
    localhost:  # Individual host
      ansible_host: localhost  # IP address or hostname
      ansible_user: kshitij  # SSH username
      ansible_ssh_pass: admin@123  # SSH password (not recommended, use SSH keys)

